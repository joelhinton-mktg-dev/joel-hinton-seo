[build]
  publish = "dist"
  command = "npm run build"

# Enable form submissions
[[forms]]
  name = "contact-form"

# Redirect rules for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200